# Antigravity-Specific Rules

- Use Python 3.10+ and PyTorch 2.x
- Always run commands in the terminal, do not simulate output
- When verifying parameter counts, actually execute the Python code
- For file modifications, show diffs before applying
- Do NOT auto-commit to git without my approval
